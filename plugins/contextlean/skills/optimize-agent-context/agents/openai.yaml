interface:
  display_name: "Optimize Agent Context"
  short_description: "Audit and safely reduce agent context overhead"
  default_prompt: "Use $optimize-agent-context to audit this agent environment and propose reversible improvements."
