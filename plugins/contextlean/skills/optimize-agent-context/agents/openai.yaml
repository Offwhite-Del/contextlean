interface:
  display_name: "Optimize Agent Context"
  short_description: "Measure and safely optimize Agent context"
  default_prompt: "Use $optimize-agent-context to measure this Agent environment, evaluate bounded candidates, and propose only evidence-backed reversible improvements."
